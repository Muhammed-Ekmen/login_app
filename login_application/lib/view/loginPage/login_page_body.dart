import 'package:flutter/material.dart';

class Login_Page_Body extends StatelessWidget {
  const Login_Page_Body({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        
      ],
    );
  }
}
